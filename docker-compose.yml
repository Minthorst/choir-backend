services:
  db:
    image: postgres:15-alpine
    container_name: choir-postgres-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: changeThis
      POSTGRES_DB: choirDB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: